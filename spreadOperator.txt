Module in javascript is just a file. every file is a module
To install library we need to have a project. 
package.json is the definition of the project. Projects in node are called packages.
short variant to skip question init -y
with package.json we get access to npm install or npm i, with which we can install libraries.
Mocha runs every file that is called test.js or or file in directory tests/.js
delete libraries with npm uninstall and the library for example:
npm uninstall mocha
it doesn't matter where you put the flag



{
  // Use IntelliSense to learn about possible attributes.
  // Hover to view descriptions of existing attributes.
  // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Mocha Tests",
      "program": "${workspaceFolder}/node_modules/mocha/bin/_mocha",
      "args": ["-u", "bdd", "--timeout", "999999", "--colors", "${file}"],
      "internalConsoleOptions": "openOnFirstSessionStart"
    }
  ]
}