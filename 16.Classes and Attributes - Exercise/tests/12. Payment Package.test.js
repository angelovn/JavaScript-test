const { expect } = require("chai");
const PaymentPackage = require("../12. Payment Package");

describe("paymentPackage Class Test", () => {
  describe("Construct Parameters Test", () => {
    let package;
    beforeEach(() => {
      package = new PaymentPackage(10, 10);
    });
    it("asda", () => {});
  });
});
